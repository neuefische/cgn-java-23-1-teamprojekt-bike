module.exports = {
   component: {
      devServer: {
         framework: 'create-react-app',
         bundler: 'webpack',
      },
   },

   projectId: 'vvu8e5',

   e2e: {
      setupNodeEvents(on, config) {
         // implement node event listeners here
      },
   },
}
